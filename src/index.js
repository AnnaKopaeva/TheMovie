import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// constants
const rootElement = document.getElementById('root');

ReactDOM.render(
  <App />,
  rootElement,
);
